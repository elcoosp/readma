---
sidebar_position: 1
---
import CodeBlock from '@theme/CodeBlock';
import readmaConfig from '!!raw-loader!./readma.config.ts';

# Introduction

Let's discover **Readma in less than 5 minutes**.

## Getting Started
### Configuration
Create a `readma.config.ts` file at the root of your project workspace.

**Export default** a config object [following this type](https://jsr.io/@readma/core/doc/~/types.ReadmeTemplateArgs)

<CodeBlock language="ts" title="readma.config.ts">{readmaConfig}</CodeBlock>

### What you'll need

- [Deno](https://deno.com/) version 2 or above
- [Node](https://nodejs.org/) (used to read the configuration file)

## Run [@readma/cli](https://github.com/elcoosp/readma/tree/main/cli)
### Generate readme(s) of your project workspace

Generate a readme per project workspace member and/or a main one by running the
following command

:::danger
Will overwrite `README.md` files, be sure to commit those before
:::

```bash
deno run -A jsr:@readma/cli gen
```
